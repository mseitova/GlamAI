OPENAI_API_KEY=sk-...
AUTH_SECRET=any-long-random-string

# Используется только при запуске npm run dev напрямую (без Docker).
# В docker-compose.yml для контейнера app DATABASE_URL подставляется автоматически.
DATABASE_URL=postgres://smile:smile@localhost:5432/smile
